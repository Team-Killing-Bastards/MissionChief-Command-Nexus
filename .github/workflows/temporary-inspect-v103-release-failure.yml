name: Temporary inspect v1.0.3 release failure

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize

permissions:
  actions: read
  contents: read

jobs:
  inspect-failure:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'agent/trigger-publish-v1.0.3'
    runs-on: ubuntu-latest

    steps:
      - name: Print failed release logs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh run view 29708913434 \
            --repo "${GITHUB_REPOSITORY}" \
            --log-failed
