name: v1.0.21 dispatch marker

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  marker:
    runs-on: ubuntu-latest
    steps:
      - name: Marker
        run: echo "v1.0.21 trusted dispatch marker"
