name: Apply runtime memory maintenance v1.0.74

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/builders/apply-runtime-memory-maintenance-v1074.py'
      - '.github/workflows/apply-runtime-memory-maintenance-v1074.yml'

permissions:
  contents: write

concurrency:
  group: apply-runtime-memory-maintenance-v1074
  cancel-in-progress: false

jobs:
  build-and-commit:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Check out exact PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Apply validated runtime memory maintenance
        run: python3 .github/builders/apply-runtime-memory-maintenance-v1074.py

      - name: Validate JavaScript syntax
        run: node --check src/missionchief-command-nexus.user.js

      - name: Run every permanent behavioural regression
        run: |
          for check in scripts/check-*.mjs; do
            echo "Running $check"
            node "$check"
          done

      - name: Validate userscript and repository
        run: |
          node scripts/validate-userscript.mjs
          python3 scripts/check_repository.py
          git diff --check

      - name: Commit validated candidate
        run: |
          if git diff --quiet --exit-code && [ -z "$(git status --porcelain)" ]; then
            echo "No candidate changes remain to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md CHANGELOG.md src scripts .github/workflows/validate-userscript.yml
          git commit -m "Harden long-session runtime memory lifecycle"
          git push origin "HEAD:${{ github.head_ref }}"
