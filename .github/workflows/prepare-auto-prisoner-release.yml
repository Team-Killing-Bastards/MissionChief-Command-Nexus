name: Prepare Auto prisoner release fallback

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  prepare:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'agent/auto-release-prisoners-final-fallback'
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out working branch
        uses: actions/checkout@v6
        with:
          ref: agent/auto-release-prisoners-final-fallback
          fetch-depth: 0
          show-progress: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Apply final prisoner release fallback
        shell: bash
        run: |
          set -euo pipefail
          python3 .diagnostics/apply-auto-prisoner-release.py
          rm -f .diagnostics/apply-auto-prisoner-release.py

      - name: Validate complete Command Nexus candidate
        shell: bash
        run: |
          set -euo pipefail
          node --check src/missionchief-command-nexus.user.js
          node scripts/validate-userscript.mjs
          python3 scripts/check_repository.py
          for check in scripts/check-*.mjs; do
            echo "Running ${check}"
            node "${check}"
          done
          test ! -e src/personnel-register-tools.user.js
          grep -Fq '// @version      1.0.42' src/missionchief-command-nexus.user.js
          grep -Fq ' * MODULE 2: MISSION FINDER V10.6.106' src/missionchief-command-nexus.user.js
          grep -Fq 'await handleAutoPrisonerReleaseAfterActions();' src/missionchief-command-nexus.user.js
          grep -Fq 'realClickForQueueRestart(releaseLink);' src/missionchief-command-nexus.user.js
          git diff --check

      - name: Commit validated integration
        shell: bash
        run: |
          set -euo pipefail
          git config user.name MartyBlyth
          git config user.email 32400596+Martyblyth@users.noreply.github.com
          git add -A
          if git diff --cached --quiet; then
            echo 'Candidate already current.'
            exit 0
          fi
          git commit -m 'Release prisoners after Auto Mode actions complete'
          git push origin HEAD:agent/auto-release-prisoners-final-fallback
