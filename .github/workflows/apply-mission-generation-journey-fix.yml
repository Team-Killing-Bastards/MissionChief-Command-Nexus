name: Apply mission generation and journey capture fix

on:
  push:
    branches:
      - fix/mission-generation-and-journey-capture
    paths:
      - .github/workflows/apply-mission-generation-journey-fix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Check out release branch
        uses: actions/checkout@v6
        with:
          ref: fix/mission-generation-and-journey-capture
          fetch-depth: 0

      - name: Patch userscript, tests and documentation
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path
          import re

          SOURCE = Path('src/missionchief-command-nexus.user.js')
          source = SOURCE.read_text(encoding='utf-8')

          def replace_once(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: expected one match, found {count}')
              return text.replace(old, new, 1)

          def function_bounds(text, name):
              marker = f'function {name}('
              start = text.find(marker)
              if start < 0:
                  raise SystemExit(f'Missing function {name}')
              parameter_start = text.find('(', start)
              depth = 0
              quote = None
              escaped = False
              body_start = -1
              i = parameter_start
              while i < len(text):
                  ch = text[i]
                  if quote:
                      if escaped:
                          escaped = False
                      elif ch == '\\':
                          escaped = True
                      elif ch == quote:
                          quote = None
                      i += 1
                      continue
                  if ch in ('"', "'", '`'):
                      quote = ch
                  elif ch == '(':
                      depth += 1
                  elif ch == ')':
                      depth -= 1
                      if depth == 0:
                          body_start = text.find('{', i)
                          break
                  i += 1
              if body_start < 0:
                  raise SystemExit(f'Missing body for {name}')

              depth = 0
              quote = None
              escaped = False
              line_comment = False
              block_comment = False
              i = body_start
              while i < len(text):
                  ch = text[i]
                  nxt = text[i + 1] if i + 1 < len(text) else ''
                  if line_comment:
                      if ch == '\n':
                          line_comment = False
                      i += 1
                      continue
                  if block_comment:
                      if ch == '*' and nxt == '/':
                          block_comment = False
                          i += 2
                          continue
                      i += 1
                      continue
                  if quote:
                      if escaped:
                          escaped = False
                      elif ch == '\\':
                          escaped = True
                      elif ch == quote:
                          quote = None
                      i += 1
                      continue
                  if ch == '/' and nxt == '/':
                      line_comment = True
                      i += 2
                      continue
                  if ch == '/' and nxt == '*':
                      block_comment = True
                      i += 2
                      continue
                  if ch in ('"', "'", '`'):
                      quote = ch
                      i += 1
                      continue
                  if ch == '{':
                      depth += 1
                  elif ch == '}':
                      depth -= 1
                      if depth == 0:
                          return start, i + 1
                  i += 1
              raise SystemExit(f'Unable to bound function {name}')

          def replace_function(text, name, replacement):
              start, end = function_bounds(text, name)
              return text[:start] + replacement.rstrip() + text[end:]

          source = replace_once(
              source,
              '// @version      1.1.2',
              '// @version      1.1.3',
              'userscript version',
          )
          source = replace_once(
              source,
              'MODULE 2: MISSION FINDER V10.7.0',
              'MODULE 2: MISSION FINDER V10.7.1',
              'Mission Finder module version',
          )
          source = replace_once(
              source,
              "const MF_MISSION_LOGGER_CLIENT_VERSION = '1.1.2';",
              "const MF_MISSION_LOGGER_CLIENT_VERSION = '1.1.3';",
              'logger client version',
          )
          source = replace_once(
              source,
              "const MF_MISSION_LOGGER_MISSION_FINDER_VERSION =\n        '10.7.0';",
              "const MF_MISSION_LOGGER_MISSION_FINDER_VERSION =\n        '10.7.1';",
              'logger Mission Finder version',
          )

          source = replace_once(
              source,
              "    const MF_MISSION_LOGGER_BATCH_SIZE = 40;\n",
              """    const MF_MISSION_LOGGER_BATCH_SIZE = 40;
    const MF_MISSION_LOGGER_OBSERVED_RETENTION_MS =
        7 * 24 * 60 * 60 * 1000;
    const MF_MISSION_LOGGER_OBSERVED_MAX_ENTRIES = 5000;
    const MF_MISSION_LOGGER_GENERATION_ARM_DELAY_MS = 1500;
""",
              'logger generation constants',
          )
          source = replace_once(
              source,
              "    let mfMissionLoggerCompletionHook = null;\n",
              """    let mfMissionLoggerCompletionHook = null;
    let mfMissionLoggerMissionMarkerWrapper = null;
    let mfMissionLoggerGenerationArmTimer = null;
    let mfMissionLoggerGenerationArmed = false;
    let mfMissionLoggerGenerationInstalled = false;
""",
              'logger generation state',
          )

          journey_reader = r'''    function readMissionLoggerUnitJourneyMetrics(source) {
        const parseNumber = value => {
            const raw = String(value ?? '')
                .replace(/\u00a0/g, ' ')
                .trim();
            if (!raw) return null;

            const normalised = raw.includes('.')
                ? raw.replace(/,/g, '')
                : raw.replace(',', '.');
            const number = Number(normalised);
            return Number.isFinite(number) && number >= 0
                ? number
                : null;
        };

        const parseDistance = value => {
            const direct = parseNumber(value);
            if (direct !== null) return direct;

            const raw = String(value ?? '')
                .replace(/\u00a0/g, ' ')
                .trim();
            const match = raw.match(
                /(\d+(?:[.,]\d+)?)\s*(km|kilomet(?:er|re)s?|m|met(?:er|re)s?)\b/i
            );
            if (!match) return null;

            const number = parseNumber(match[1]);
            if (number === null) return null;
            return /^k/i.test(match[2])
                ? number
                : number / 1000;
        };

        const parseEta = value => {
            const direct = parseNumber(value);
            if (direct !== null) return direct;

            const raw = String(value ?? '')
                .replace(/\u00a0/g, ' ')
                .trim()
                .toLowerCase();
            if (!raw) return null;

            const clock = raw.match(
                /(?:^|\D)(\d{1,3}):([0-5]\d)(?::([0-5]\d))?(?:\D|$)/
            );
            if (clock) {
                if (clock[3] !== undefined) {
                    return (
                        Number(clock[1]) * 3600 +
                        Number(clock[2]) * 60 +
                        Number(clock[3])
                    );
                }
                return Number(clock[1]) * 60 + Number(clock[2]);
            }

            let total = 0;
            let matched = false;
            for (const part of raw.matchAll(
                /(\d+(?:[.,]\d+)?)\s*(hours?|hrs?|hr|h|minutes?|mins?|min|seconds?|secs?|sec|s)\b/g
            )) {
                const number = parseNumber(part[1]);
                if (number === null) continue;
                matched = true;
                const unit = part[2];
                if (/^h/.test(unit)) total += number * 3600;
                else if (/^m/.test(unit)) total += number * 60;
                else total += number;
            }
            return matched ? total : null;
        };

        const row = source?.closest?.('tr') ||
            (source?.matches?.('tr') ? source : null);
        const distanceNodes = [];
        const etaNodes = [];
        const textNodes = [];
        const addNode = (collection, node) => {
            if (node && !collection.includes(node)) {
                collection.push(node);
            }
        };

        [source, row].forEach(node => {
            addNode(distanceNodes, node);
            addNode(etaNodes, node);
            addNode(textNodes, node);
        });

        const collect = (root, selectors, collection) => {
            if (!root?.querySelectorAll) return;
            selectors.forEach(selector => {
                try {
                    Array.from(root.querySelectorAll(selector))
                        .slice(0, 30)
                        .forEach(node => addNode(collection, node));
                } catch (_error) {}
            });
        };

        [source, row].forEach(root => {
            collect(
                root,
                [
                    '[data-distance-km]',
                    '[data-route-distance-km]',
                    '[data-route-distance]',
                    '[data-distance]',
                    '[distance]'
                ],
                distanceNodes
            );
            collect(
                root,
                [
                    '[data-eta-seconds]',
                    '[data-arrival-seconds]',
                    '[data-duration-seconds]',
                    '[data-timevalue]',
                    '[timevalue]',
                    '[data-sortvalue]',
                    '[data-eta]'
                ],
                etaNodes
            );
            collect(
                root,
                [
                    '.vehicle_distance',
                    '.vehicle-distance',
                    '[class*="distance"]',
                    '.vehicle_arrival_time',
                    '.vehicle-arrival-time',
                    '.vehicle_eta',
                    '.vehicle-eta',
                    '[class*="arrival"]',
                    '[class*="eta"]',
                    'td'
                ],
                textNodes
            );
        });

        const readAttribute = (nodes, names, parser) => {
            for (const node of nodes) {
                for (const name of names) {
                    const value = node?.getAttribute?.(name);
                    if (value === null || value === undefined || value === '') {
                        continue;
                    }
                    const parsed = parser(value);
                    if (parsed !== null) return parsed;
                }
            }
            return null;
        };

        let distanceKm = readAttribute(
            distanceNodes,
            [
                'data-distance-km',
                'data-route-distance-km',
                'data-route-distance',
                'data-distance',
                'distance'
            ],
            parseDistance
        );
        let etaSeconds = readAttribute(
            etaNodes,
            [
                'data-eta-seconds',
                'data-arrival-seconds',
                'data-duration-seconds',
                'data-timevalue',
                'timevalue',
                'data-sortvalue',
                'data-eta'
            ],
            parseEta
        );

        const readText = parser => {
            for (const node of textNodes) {
                for (const value of [
                    node?.getAttribute?.('aria-label'),
                    node?.getAttribute?.('title'),
                    node?.getAttribute?.('data-content'),
                    node?.textContent
                ]) {
                    const parsed = parser(value);
                    if (parsed !== null) return parsed;
                }
            }
            return null;
        };

        if (distanceKm === null) {
            distanceKm = readText(parseDistance);
        }
        if (etaSeconds === null) {
            etaSeconds = readText(parseEta);
        }

        if (distanceKm !== null && distanceKm > 100000) {
            distanceKm = null;
        }
        if (etaSeconds !== null && etaSeconds > 604800) {
            etaSeconds = null;
        }

        return {
            estimatedDistanceKm:
                distanceKm === null
                    ? null
                    : Math.round(distanceKm * 1000) / 1000,
            estimatedEtaSeconds:
                etaSeconds === null
                    ? null
                    : Math.round(etaSeconds)
        };
    }'''
          source = replace_function(
              source,
              'readMissionLoggerUnitJourneyMetrics',
              journey_reader,
          )
          source = replace_once(
              source,
              'readMissionLoggerUnitJourneyMetrics(row);',
              'readMissionLoggerUnitJourneyMetrics(checkbox);',
              'selected-unit journey source',
          )

          sort_reader = r'''    function sortVehicleCheckboxesByBestArrival(matches) {
        const getMetrics = input => {
            const now = Date.now();
            const cached = mfVehicleArrivalMetricCache.get(input);

            if (cached && now < cached.expiresAt) {
                return cached;
            }

            const journey =
                readMissionLoggerUnitJourneyMetrics(input);
            const metrics = {
                delay:
                    journey.estimatedEtaSeconds ?? 999999,
                distance:
                    journey.estimatedDistanceKm ?? 999999,
                expiresAt: now + 500
            };

            mfVehicleArrivalMetricCache.set(input, metrics);
            return metrics;
        };

        matches.sort((a, b) => {
            const metricsA = getMetrics(a);
            const metricsB = getMetrics(b);
            return (
                metricsA.delay - metricsB.delay ||
                metricsA.distance - metricsB.distance
            );
        });

        return matches;
    }'''
          source = replace_function(
              source,
              'sortVehicleCheckboxesByBestArrival',
              sort_reader,
          )

          generation_functions = r'''    function writeMissionLoggerObservedRegistry(registry) {
        const now = Date.now();
        const entries = Object.entries(
            registry && typeof registry === 'object'
                ? registry
                : {}
        )
            .filter(([, timestamp]) => {
                const value = Number(timestamp || 0);
                return (
                    value > 0 &&
                    now - value <=
                        MF_MISSION_LOGGER_OBSERVED_RETENTION_MS
                );
            })
            .sort((left, right) => {
                return Number(right[1]) - Number(left[1]);
            })
            .slice(0, MF_MISSION_LOGGER_OBSERVED_MAX_ENTRIES);

        localStorage.setItem(
            MF_MISSION_LOGGER_OBSERVED_KEY,
            JSON.stringify(Object.fromEntries(entries))
        );
    }

    function getMissionLoggerCurrentUserId() {
        for (const candidate of getMissionLoggerSameOriginWindows()) {
            for (const value of [
                candidate?.user_id,
                candidate?.current_user_id,
                candidate?.currentUser?.id,
                candidate?.user?.id
            ]) {
                const id = String(value ?? '').trim();
                if (/^\d+$/.test(id)) return id;
            }
        }

        try {
            const userLink = document.querySelector(
                '#navbar-main-user a[href*="/profile/"], ' +
                'a.navbar-user[href*="/profile/"]'
            );
            const match = String(
                userLink?.getAttribute?.('href') || ''
            ).match(/\/profile\/(\d+)/i);
            if (match) return match[1];
        } catch (_error) {}

        return '';
    }

    function getMissionLoggerMissionListRows(root) {
        const rows = [];
        const selector = [
            '#mission_list .missionSideBarEntry[id^="mission_"]',
            '#mission_list .missionSideBarEntry[mission_id]',
            '.missionSideBarEntry[id^="mission_"]',
            '.missionSideBarEntry[mission_id]'
        ].join(',');
        const add = row => {
            if (row && !rows.includes(row)) rows.push(row);
        };

        try {
            if (root?.matches?.(selector)) add(root);
        } catch (_error) {}
        try {
            Array.from(root?.querySelectorAll?.(selector) || [])
                .forEach(add);
        } catch (_error) {}

        return rows;
    }

    function getMissionLoggerMissionListRecord(
        suppliedMission,
        row
    ) {
        const missionId = (() => {
            for (const value of [
                suppliedMission?.id,
                suppliedMission?.mission_id,
                row?.getAttribute?.('mission_id'),
                row?.getAttribute?.('data-mission-id'),
                String(row?.id || '').match(/^mission_(\d+)$/)?.[1]
            ]) {
                const id = String(value ?? '').trim();
                if (/^\d+$/.test(id)) return id;
            }
            return '';
        })();
        if (!missionId) return null;

        let mission = suppliedMission &&
            typeof suppliedMission === 'object'
                ? suppliedMission
                : null;
        if (!mission) {
            for (const candidate of getMissionLoggerSameOriginWindows()) {
                try {
                    mission = getMissionLoggerCacheRecord(
                        candidate.missions_data,
                        missionId
                    );
                } catch (_error) {}
                if (mission) break;
            }
        }

        const currentUserId = getMissionLoggerCurrentUserId();
        const ownerId = (() => {
            for (const value of [
                mission?.user_id,
                mission?.userId,
                mission?.owner_id,
                mission?.ownerId,
                row?.getAttribute?.('user_id'),
                row?.getAttribute?.('data-user-id'),
                row?.getAttribute?.('owner_id'),
                row?.getAttribute?.('data-owner-id')
            ]) {
                const id = String(value ?? '').trim();
                if (/^\d+$/.test(id)) return id;
            }
            return '';
        })();

        if (
            !currentUserId ||
            !ownerId ||
            ownerId !== currentUserId
        ) {
            return null;
        }

        const numberOrNull = value => {
            if (value === null || value === undefined || value === '') {
                return null;
            }
            const number = Number(value);
            return Number.isFinite(number) && number >= 0
                ? number
                : null;
        };
        let sortable = {};
        try {
            sortable = JSON.parse(
                row?.getAttribute?.('data-sortable-by') ||
                row?.getAttribute?.('data-sortable_by') ||
                '{}'
            );
        } catch (_error) {}

        const missionName = trimMissionLoggerText(
            mission?.caption ||
            mission?.name ||
            mission?.title ||
            row?.querySelector?.(
                '.missionSideBarEntrySearchHelper, ' +
                '.mission_caption, a[href*="/missions/"]'
            )?.textContent ||
            '',
            240
        );
        const missionDefinitionId = (() => {
            for (const value of [
                mission?.mtid,
                mission?.mission_type_id,
                mission?.missionTypeId,
                row?.getAttribute?.('mission_type_id'),
                row?.getAttribute?.('data-mission-type-id')
            ]) {
                const id = String(value ?? '').trim();
                if (/^\d+$/.test(id)) return id;
            }
            return '';
        })();
        const generatorStationId = (() => {
            for (const value of [
                mission?.generator_building_id,
                mission?.generated_by_building_id,
                mission?.building_id
            ]) {
                const id = String(value ?? '').trim();
                if (/^\d+$/.test(id)) return id;
            }
            return '';
        })();
        const generatorStationName = trimMissionLoggerText(
            mission?.generator_building_name ||
            mission?.generated_by_name ||
            mission?.generated_by ||
            (
                generatorStationId
                    ? getMissionLoggerBuildingName(
                        generatorStationId
                    )
                    : ''
            ),
            180
        );
        const allianceId = String(
            mission?.alliance_id ??
            mission?.allianceId ??
            row?.getAttribute?.('alliance_id') ??
            row?.getAttribute?.('data-alliance-id') ??
            ''
        ).trim();

        return {
            missionId,
            missionDefinitionId,
            missionName,
            missionUrl:
                `${window.location.origin}/missions/${missionId}`,
            ownership: 'own',
            shared:
                allianceId !== '' &&
                allianceId !== '0' &&
                allianceId !== 'null',
            advertisedCredits: Math.max(
                0,
                numberOrNull(
                    mission?.average_credits ??
                    mission?.advertised_credits ??
                    sortable.average_credits
                ) || 0
            ),
            patientCount: Math.max(
                0,
                numberOrNull(
                    mission?.patients_count ??
                    sortable.patients_count?.[0] ??
                    sortable.patients_count
                ) || 0
            ),
            possiblePatientCount: Math.max(
                0,
                numberOrNull(
                    mission?.possible_patients_count ??
                    sortable.patients_count?.[1]
                ) || 0
            ),
            prisonerCount: numberOrNull(
                mission?.prisoners_count ??
                sortable.prisoners_count?.[0] ??
                sortable.prisoners_count
            ),
            possiblePrisonerCount: numberOrNull(
                mission?.possible_prisoners_count ??
                sortable.prisoners_count?.[1]
            ),
            generatorStationId,
            generatorStationName,
            generatorSource:
                generatorStationId || generatorStationName
                    ? 'mission-marker'
                    : ''
        };
    }

    function createMissionLoggerGeneratedEvent(record) {
        const identity = readMissionLoggerIdentity();
        if (!identity || !record?.missionId) return null;

        const capturedAt = new Date().toISOString();
        const missionRegistry = readMissionLoggerMissionRegistry();
        const previousMission = missionRegistry[
            createMissionLoggerRegistryKey(
                identity.playerId,
                record.missionId
            )
        ] || {};

        return {
            schemaVersion:
                MF_MISSION_LOGGER_SCHEMA_VERSION,
            eventId: createMissionLoggerId('event'),
            eventType: 'mission-observed',
            capturedAt,
            playerId: identity.playerId,
            deviceId: identity.deviceId,
            missionId: record.missionId,
            missionDefinitionId:
                record.missionDefinitionId || '',
            missionName: record.missionName || '',
            missionUrl: record.missionUrl || '',
            ownership: 'own',
            generatorStationId:
                record.generatorStationId || '',
            generatorStationName:
                record.generatorStationName || '',
            dispatchMode: '',
            shared: record.shared === true,
            advertisedCredits: Math.max(
                0,
                Number(record.advertisedCredits || 0)
            ),
            actualCredits: null,
            patientCount: Math.max(
                0,
                Number(record.patientCount || 0)
            ),
            prisonerCount:
                record.prisonerCount,
            transportCount: null,
            requirements: [],
            units: [],
            metadata: {
                source: 'missionchief-command-nexus',
                observationSource:
                    'mission-list-generated',
                ready: false,
                reason: '',
                autoMode: false,
                dispatchFingerprint: '',
                generatorSource:
                    record.generatorSource || '',
                missionSnapshotSource:
                    'mission-list-generated',
                possiblePatientCount: Math.max(
                    0,
                    Number(
                        record.possiblePatientCount || 0
                    )
                ),
                possiblePrisonerCount:
                    record.possiblePrisonerCount,
                firstObservedAt: earlierMissionLoggerIso(
                    previousMission.firstObservedAt,
                    capturedAt
                ),
                firstUnitSentAt:
                    previousMission.firstUnitSentAt || '',
                dispatchCount: Math.max(
                    0,
                    Number(previousMission.dispatchCount || 0)
                ),
                unitCount: Math.max(
                    0,
                    Number(previousMission.unitCount || 0)
                ),
                missionFinderVersion:
                    MF_MISSION_LOGGER_MISSION_FINDER_VERSION
            }
        };
    }

    function recordMissionLoggerGeneratedMission(
        suppliedMission,
        row,
        options = {}
    ) {
        if (!MF_IS_TOP_WINDOW) return false;

        const record = getMissionLoggerMissionListRecord(
            suppliedMission,
            row
        );
        if (!record) return false;

        const now = Date.now();
        const registry = readMissionLoggerObservedRegistry();
        const existingAt = Number(
            registry[record.missionId] || 0
        );
        if (
            existingAt > 0 &&
            now - existingAt <=
                MF_MISSION_LOGGER_OBSERVED_RETENTION_MS
        ) {
            return false;
        }

        if (
            options.baselineOnly === true ||
            !mfMissionLoggerGenerationArmed ||
            !mfMissionLoggerEnabled ||
            !readMissionLoggerIdentity()
        ) {
            registry[record.missionId] = now;
            writeMissionLoggerObservedRegistry(registry);
            return false;
        }

        const queued = queueMissionLoggerEvent(
            createMissionLoggerGeneratedEvent(record)
        );
        if (!queued) return false;

        registry[record.missionId] = now;
        writeMissionLoggerObservedRegistry(registry);
        return true;
    }

    function scanMissionLoggerMissionList(
        root,
        baselineOnly
    ) {
        let count = 0;
        getMissionLoggerMissionListRows(root).forEach(row => {
            if (
                recordMissionLoggerGeneratedMission(
                    null,
                    row,
                    { baselineOnly: baselineOnly === true }
                )
            ) {
                count += 1;
            }
        });
        return count;
    }

    function scheduleMissionLoggerGenerationArm() {
        if (mfMissionLoggerGenerationArmed) return;
        if (mfMissionLoggerGenerationArmTimer) {
            clearTimeout(mfMissionLoggerGenerationArmTimer);
        }

        mfMissionLoggerGenerationArmTimer = setTimeout(
            () => {
                mfMissionLoggerGenerationArmTimer = null;
                scanMissionLoggerMissionList(
                    document,
                    true
                );
                mfMissionLoggerGenerationArmed = true;
            },
            MF_MISSION_LOGGER_GENERATION_ARM_DELAY_MS
        );
    }

    function installMissionLoggerMissionGenerationCapture() {
        if (!MF_IS_TOP_WINDOW) return;

        if (!mfMissionLoggerGenerationInstalled) {
            mfMissionLoggerGenerationInstalled = true;
            scanMissionLoggerMissionList(document, true);
            scheduleMissionLoggerGenerationArm();
        }

        const nativeMissionMarkerAdd =
            window.missionMarkerAdd;
        if (
            typeof nativeMissionMarkerAdd !== 'function' ||
            nativeMissionMarkerAdd ===
                mfMissionLoggerMissionMarkerWrapper ||
            nativeMissionMarkerAdd
                .__mfMissionLoggerGenerationWrapper === true
        ) {
            return;
        }

        const wrapper = function(...args) {
            const result = nativeMissionMarkerAdd.apply(
                this,
                args
            );
            try {
                recordMissionLoggerGeneratedMission(
                    args[0],
                    null,
                    {
                        baselineOnly:
                            !mfMissionLoggerGenerationArmed
                    }
                );
            } catch (_error) {}
            return result;
        };
        try {
            Object.defineProperty(
                wrapper,
                '__mfMissionLoggerGenerationWrapper',
                { value: true }
            );
        } catch (_error) {}

        mfMissionLoggerMissionMarkerWrapper = wrapper;
        window.missionMarkerAdd = wrapper;
    }

    function observeMissionLoggerMissionListMutations(records) {
        if (!MF_IS_TOP_WINDOW) return;

        installMissionLoggerMissionGenerationCapture();
        let sawMissionRows = false;
        (records || []).forEach(record => {
            Array.from(record?.addedNodes || [])
                .forEach(node => {
                    const rows =
                        getMissionLoggerMissionListRows(node);
                    if (rows.length === 0) return;
                    sawMissionRows = true;
                    rows.forEach(row => {
                        recordMissionLoggerGeneratedMission(
                            null,
                            row,
                            {
                                baselineOnly:
                                    !mfMissionLoggerGenerationArmed
                            }
                        );
                    });
                });
        });

        if (
            sawMissionRows &&
            !mfMissionLoggerGenerationArmed
        ) {
            scheduleMissionLoggerGenerationArm();
        }
    }

'''
          record_start, _ = function_bounds(
              source,
              'recordMissionLoggerObservedEvent',
          )
          source = (
              source[:record_start] +
              generation_functions +
              source[record_start:]
          )

          observed = r'''    function recordMissionLoggerObservedEvent() {
        if (
            !mfMissionLoggerEnabled ||
            !readMissionLoggerIdentity() ||
            !isMissionPage()
        ) {
            return false;
        }

        const missionId =
            getCurrentMissionIdForQueueRestart();
        if (!missionId) return false;

        const now = Date.now();
        const registry = readMissionLoggerObservedRegistry();
        const key = String(missionId);

        if (
            now - Number(registry[key] || 0) <=
            MF_MISSION_LOGGER_OBSERVED_RETENTION_MS
        ) {
            return false;
        }

        const queued = queueMissionLoggerEvent(
            createMissionLoggerEvent(
                'mission-observed',
                { includeUnits: false }
            )
        );
        if (!queued) return false;

        registry[key] = now;
        writeMissionLoggerObservedRegistry(registry);
        return true;
    }'''
          source = replace_function(
              source,
              'recordMissionLoggerObservedEvent',
              observed,
          )

          source = replace_once(
              source,
              """        if (MF_IS_TOP_WINDOW) {
            startMissionLoggerSyncTimer();
        }
""",
              """        if (MF_IS_TOP_WINDOW) {
            installMissionLoggerMissionGenerationCapture();
            startMissionLoggerSyncTimer();
        }
""",
              'top-window logger startup',
          )
          source = replace_once(
              source,
              """        mfMainMutationObserver = new MutationObserver(records => {
            releaseRemovedMissionFinderFrameRuntimes(records);
            scheduleMissionFinderMutationWork(records);
        });
""",
              """        mfMainMutationObserver = new MutationObserver(records => {
            releaseRemovedMissionFinderFrameRuntimes(records);
            if (MF_IS_TOP_WINDOW) {
                observeMissionLoggerMissionListMutations(records);
            }
            scheduleMissionFinderMutationWork(records);
        });
""",
              'existing top-window mutation observer integration',
          )

          source = replace_once(
              source,
              "Records mission identifiers/URLs, advertised and exact awarded value, requirements, current casualty counts, available generator information, selected vehicle IDs/types/names and MissionChief's dispatch-time estimated route distance/ETA for each selected unit.",
              "Records each new mission generated for this player from MissionChief's native mission list, plus mission identifiers/URLs, advertised and exact awarded value, requirements, current casualty counts, available generator information, selected vehicle IDs/types/names and MissionChief's dispatch-time estimated route distance/ETA for each selected unit.",
              'logger disclosure',
          )
          SOURCE.write_text(source, encoding='utf-8')

          # Strengthen the permanent journey regression.
          journey_test = Path('scripts/check-mission-journey-metrics.mjs')
          journey = journey_test.read_text(encoding='utf-8')
          old_helper = """const row = attributes => ({
  getAttribute(name) {
    return Object.prototype.hasOwnProperty.call(attributes, name)
      ? attributes[name]
      : null;
  },
});
"""
          new_helper = """const node = (attributes = {}, descendants = [], textContent = '') => ({
  textContent,
  getAttribute(name) {
    return Object.prototype.hasOwnProperty.call(attributes, name)
      ? attributes[name]
      : null;
  },
  querySelectorAll() {
    return descendants;
  },
  matches(selector) {
    return selector === 'tr' && attributes.__row === true;
  },
  closest() {
    return attributes.__closest || null;
  },
});
const row = attributes => node(attributes);
"""
          journey = replace_once(
              journey,
              old_helper,
              new_helper,
              'journey test DOM helper',
          )
          missing_anchor = """const missing = readMetrics(row({}));
expect(missing.estimatedDistanceKm === null, 'Missing distance must remain blank');
expect(missing.estimatedEtaSeconds === null, 'Missing ETA must remain blank');
"""
          extended = missing_anchor + """
const nestedDistance = node({ 'data-distance-km': '12.75' });
const nestedEta = node({ 'data-arrival-seconds': '420' });
const nestedRow = node({ __row: true }, [nestedDistance, nestedEta]);
const nestedCheckbox = node({ __closest: nestedRow });
const nested = readMetrics(nestedCheckbox);
expect(nested.estimatedDistanceKm === 12.75, 'Journey capture must read distance from a native descendant metric cell');
expect(nested.estimatedEtaSeconds === 420, 'Journey capture must read ETA from a native descendant metric cell');

const visibleEvidence = node(
  { title: 'Route distance 3.4 km; ETA 7 min' },
  [],
  ''
);
const visibleRow = node({ __row: true }, [visibleEvidence]);
const visibleCheckbox = node({ __closest: visibleRow });
const visible = readMetrics(visibleCheckbox);
expect(visible.estimatedDistanceKm === 3.4, 'Explicit kilometre text must remain valid native journey evidence');
expect(visible.estimatedEtaSeconds === 420, 'Explicit ETA minute text must convert to seconds');
"""
          journey = replace_once(
              journey,
              missing_anchor,
              extended,
              'journey nested evidence tests',
          )
          journey = replace_once(
              journey,
              "'readMissionLoggerUnitJourneyMetrics(row)',",
              "'readMissionLoggerUnitJourneyMetrics(checkbox)',",
              'selected unit shared journey reader expectation',
          )
          anchor = """const selectedUnits = extractFunction(source, 'getMissionLoggerSelectedUnits');
"""
          addition = """const arrivalSort = extractFunction(source, 'sortVehicleCheckboxesByBestArrival');
expect(
  arrivalSort.includes('readMissionLoggerUnitJourneyMetrics(input)'),
  'Vehicle arrival sorting and logger capture must use the same native metric reader'
);
expect(
  !arrivalSort.includes("getAttribute('data-distance')"),
  'Arrival sorting must not retain the old row-only distance path'
);

"""
          journey = replace_once(
              journey,
              anchor,
              addition + anchor,
              'shared arrival reader regression',
          )
          journey_test.write_text(journey, encoding='utf-8')

          generation_test = Path('scripts/check-mission-generation-logger.mjs')
          generation_test.write_text(r'''#!/usr/bin/env node

import { readFile } from 'node:fs/promises';

const source = await readFile(
  'src/missionchief-command-nexus.user.js',
  'utf8'
);

function fail(message) {
  console.error(`ERROR: ${message}`);
  process.exit(1);
}

function expect(condition, message) {
  if (!condition) fail(message);
}

function extractFunction(text, name) {
  const marker = `function ${name}(`;
  const start = text.indexOf(marker);
  if (start < 0) fail(`Missing function ${name}`);
  const parameterStart = text.indexOf('(', start);
  let parameterDepth = 0;
  let bodyStart = -1;
  let quote = '';
  let escaped = false;

  for (let index = parameterStart; index < text.length; index += 1) {
    const character = text[index];
    if (quote) {
      if (escaped) escaped = false;
      else if (character === '\\') escaped = true;
      else if (character === quote) quote = '';
      continue;
    }
    if (character === '"' || character === "'" || character === '`') {
      quote = character;
      continue;
    }
    if (character === '(') parameterDepth += 1;
    if (character === ')') {
      parameterDepth -= 1;
      if (parameterDepth === 0) {
        bodyStart = text.indexOf('{', index);
        break;
      }
    }
  }

  if (bodyStart < 0) fail(`Missing body for ${name}`);

  let depth = 0;
  quote = '';
  escaped = false;
  for (let index = bodyStart; index < text.length; index += 1) {
    const character = text[index];
    const next = text[index + 1] || '';
    if (quote) {
      if (escaped) escaped = false;
      else if (character === '\\') escaped = true;
      else if (character === quote) quote = '';
      continue;
    }
    if (character === '"' || character === "'" || character === '`') {
      quote = character;
      continue;
    }
    if (character === '/' && next === '/') {
      const end = text.indexOf('\n', index + 2);
      index = end < 0 ? text.length : end;
      continue;
    }
    if (character === '/' && next === '*') {
      const end = text.indexOf('*/', index + 2);
      if (end < 0) fail(`Unclosed comment in ${name}`);
      index = end + 1;
      continue;
    }
    if (character === '{') depth += 1;
    if (character === '}') {
      depth -= 1;
      if (depth === 0) return text.slice(start, index + 1);
    }
  }

  fail(`Unable to isolate ${name}`);
}

expect(source.includes('// @version      1.1.3'), 'Release version must be 1.1.3');
expect(source.includes('MODULE 2: MISSION FINDER V10.7.1'), 'Mission Finder must be V10.7.1');
expect(source.includes("const MF_MISSION_LOGGER_CLIENT_VERSION = '1.1.3';"), 'Logger client version must follow the release');
expect(source.includes("'10.7.1';"), 'Logger Mission Finder version must follow the engine');

const listRecord = extractFunction(
  source,
  'getMissionLoggerMissionListRecord'
);
for (const token of [
  'mission?.user_id',
  'getMissionLoggerCurrentUserId()',
  'ownerId !== currentUserId',
  'mission?.caption',
  'mission?.mtid',
  'mission?.alliance_id',
]) {
  expect(listRecord.includes(token), `Generated-mission ownership/data capture missing ${token}`);
}

const generatedEvent = extractFunction(
  source,
  'createMissionLoggerGeneratedEvent'
);
for (const token of [
  "eventType: 'mission-observed'",
  "'mission-list-generated'",
  'missionDefinitionId:',
  'advertisedCredits:',
  'firstObservedAt:',
]) {
  expect(generatedEvent.includes(token), `Generated mission event missing ${token}`);
}

const generatedRecord = extractFunction(
  source,
  'recordMissionLoggerGeneratedMission'
);
for (const token of [
  'MF_MISSION_LOGGER_OBSERVED_RETENTION_MS',
  'options.baselineOnly === true',
  '!mfMissionLoggerGenerationArmed',
  'writeMissionLoggerObservedRegistry(registry)',
  'queueMissionLoggerEvent(',
]) {
  expect(generatedRecord.includes(token), `Generated mission dedupe/baseline guard missing ${token}`);
}
expect(
  generatedRecord.indexOf('const queued = queueMissionLoggerEvent(') <
    generatedRecord.lastIndexOf('registry[record.missionId] = now'),
  'Generated missions must enter the bounded queue before their dedupe timestamp is committed'
);

const installer = extractFunction(
  source,
  'installMissionLoggerMissionGenerationCapture'
);
for (const token of [
  'window.missionMarkerAdd',
  'nativeMissionMarkerAdd.apply(',
  'return result;',
  '__mfMissionLoggerGenerationWrapper',
  'scanMissionLoggerMissionList(document, true)',
]) {
  expect(installer.includes(token), `Native missionMarkerAdd capture missing ${token}`);
}

const mutationFallback = extractFunction(
  source,
  'observeMissionLoggerMissionListMutations'
);
expect(
  mutationFallback.includes('record?.addedNodes'),
  'Existing mutation records must supply the fallback mission-list capture'
);
expect(
  !mutationFallback.includes('new MutationObserver'),
  'Mission generation capture must not create another repeating observer'
);

const observer = extractFunction(source, 'startMissionFinderObserver');
expect(
  observer.includes('installMissionLoggerMissionGenerationCapture()'),
  'Top-window startup must install generated-mission capture'
);
expect(
  observer.includes('observeMissionLoggerMissionListMutations(records)'),
  'The existing main observer must own the mission-list fallback'
);

const openedObservation = extractFunction(
  source,
  'recordMissionLoggerObservedEvent'
);
expect(
  openedObservation.includes('MF_MISSION_LOGGER_OBSERVED_RETENTION_MS'),
  'Opening a generated mission must reuse the same seven-day dedupe registry'
);

console.log(
  'Mission generation logger regression passed: current-user native mission-list creation, initial hydration baseline, callback/mutation dedupe and existing-observer ownership are locked.'
);
''', encoding='utf-8')

          # Promote the accumulated maintenance fixes and this feature into v1.1.3.
          changelog = Path('CHANGELOG.md')
          change = changelog.read_text(encoding='utf-8')
          old_unreleased = """## [Unreleased]

### Fixed

- Preserved Google Sheets dashboard and analysis formula references during logger rebuilds by clearing source data cells instead of deleting the referenced rows.
- Blocked duplicate active player display names and directs additional browsers to **Create another device pairing**, preventing ambiguous dashboard player filters.

## [1.1.2] - 2026-08-17
"""
          new_unreleased = """## [Unreleased]

No changes have been queued after `1.1.3`.

## [1.1.3] - 2026-08-17

### Added

- Added true generated-mission capture for the opt-in Mission Analytics Logger. Nexus now records each new mission belonging to the signed-in player when MissionChief adds it to the native mission list, without requiring that mission's detail page to be opened first.
- Reused MissionChief's native `missionMarkerAdd` callback and the existing top-window mutation observer as a bounded fallback. Initial mission-list hydration is recorded only as a local baseline, preventing a refresh or userscript update from falsely importing the whole existing mission list as newly generated.

### Fixed

- Fixed dispatch journey evidence remaining blank because MissionChief may expose distance and ETA on the selected checkbox or a native metric cell rather than the enclosing row. One shared reader now checks those exact native attributes and explicit unit-labelled values, and vehicle arrival sorting uses the same evidence path.
- Preserved Google Sheets dashboard and analysis formula references during logger rebuilds by clearing source data cells instead of deleting the referenced rows.
- Blocked duplicate active player display names and directs additional browsers to **Create another device pairing**, preventing ambiguous dashboard player filters.

### Security and compatibility

- Generated-mission capture accepts only a mission whose native owner ID exactly matches the current signed-in user. Alliance missions owned by another player and records without exact ownership evidence fail closed.
- Preserved the existing logger endpoint, player/browser pairings, local queue, spreadsheet and dashboard. Historical journey rows with missing evidence cannot be reconstructed; new distance/ETA evidence begins with dispatches captured by `1.1.3`.
- Increased the unified userscript from `1.1.2` to `1.1.3` and Mission Finder from `V10.7.0` to `V10.7.1`. Resource Administration remains `V4.2.8`, Unit Naming remains `3.3.27`, Station Naming remains `1.3.22`, and Personnel Assignment remains `1.3.12`.

## [1.1.2] - 2026-08-17
"""
          change = replace_once(
              change,
              old_unreleased,
              new_unreleased,
              'v1.1.3 changelog section',
          )
          changelog.write_text(change, encoding='utf-8')

          # Current operational documentation only; historical release records stay immutable.
          replacements = {
              'README.md': [
                  ('**Current version:** `1.1.2` · **Mission Finder engine:** `V10.7.0`', '**Current version:** `1.1.3` · **Mission Finder engine:** `V10.7.1`'),
                  ('Version `1.1.2` extends', 'Version `1.1.3` extends'),
                  ('with per-unit dispatch-time estimated route distance and ETA, compact long-term station journey evidence, and dashboard views for spotting stations that repeatedly send resources too far.', 'with true current-player mission-generation capture, repaired per-unit dispatch-time route distance/ETA evidence, compact long-term station journey evidence, and dashboard views for mission demand and station placement.'),
              ],
              'docs/ARCHITECTURE.md': [
                  ('current MissionChief Command Nexus v1.1.2 production source', 'current MissionChief Command Nexus v1.1.3 production source'),
                  ('Advertised mission value and casualty counts use MissionChief\'s mission-list record,', 'New own-player mission demand is captured when MissionChief adds the native mission marker/list entry, while advertised mission value and casualty counts use MissionChief\'s mission-list record,'),
              ],
              'docs/DEVELOPER_HANDOFF.md': [
                  ('| Command Nexus version | `1.1.2` |', '| Command Nexus version | `1.1.3` |'),
                  ('| Mission Finder baseline | `V10.7.0` |', '| Mission Finder baseline | `V10.7.1` |'),
                  ('captures exact dispatch selections plus MissionChief\'s available dispatch-time route distance/ETA', 'captures each exact current-player mission generation plus exact dispatch selections and MissionChief\'s available dispatch-time route distance/ETA'),
              ],
              'docs/MIGRATION.md': [
                  ('current Command Nexus `1.1.2` source', 'current Command Nexus `1.1.3` source'),
              ],
              'docs/README.md': [
                  ('Command Nexus `1.1.2` with Mission Finder `V10.7.0`', 'Command Nexus `1.1.3` with Mission Finder `V10.7.1`'),
              ],
              'docs/ROADMAP.md': [
                  ('## Current production baseline — v1.1.2', '## Current production baseline — v1.1.3'),
                  ('Release Command Nexus `1.1.2` with Mission Finder `V10.7.0`.', 'Release Command Nexus `1.1.3` with Mission Finder `V10.7.1`.'),
                  ('- [x] Capture MissionChief\'s available dispatch-time route distance/ETA per selected unit and retain compact weekly station journey aggregates for placement analysis.', '- [x] Capture MissionChief\'s available dispatch-time route distance/ETA per selected unit and retain compact weekly station journey aggregates for placement analysis.\n- [x] Capture each newly generated current-player mission from the native mission list without opening its detail page.'),
              ],
              'src/README.md': [
                  ('| Command Nexus version | `1.1.2` |', '| Command Nexus version | `1.1.3` |'),
                  ('| Mission Finder baseline | `V10.7.0` |', '| Mission Finder baseline | `V10.7.1` |'),
                  ('a bounded local outbox, per-unit dispatch distance/ETA evidence', 'a bounded local outbox, true own-player generated-mission capture and per-unit dispatch distance/ETA evidence'),
              ],
          }
          for filename, pairs in replacements.items():
              path = Path(filename)
              text = path.read_text(encoding='utf-8')
              for old, new in pairs:
                  if old not in text:
                      raise SystemExit(f'{filename}: missing documentation anchor {old!r}')
                  text = text.replace(old, new, 1)
              path.write_text(text, encoding='utf-8')

          # Remove every one-use extraction/build artifact before repository checks.
          for path in [
              Path('logger-source-snapshot.txt'),
              Path('logger-event-builder-snapshot.txt'),
              Path('.github/workflows/apply-mission-generation-journey-fix.yml'),
          ]:
              if path.exists():
                  path.unlink()
          PY

      - name: Validate JavaScript syntax
        shell: bash
        run: node -e "new Function(require('fs').readFileSync('src/missionchief-command-nexus.user.js','utf8'))"

      - name: Run targeted logger regressions
        shell: bash
        run: |
          node scripts/check-mission-generation-logger.mjs
          node scripts/check-mission-journey-metrics.mjs
          node scripts/check-mission-user-logger.mjs
          node scripts/check-logger-dashboard-rebuild-safety.mjs

      - name: Run all permanent regressions
        shell: bash
        run: |
          for test in scripts/check-*.mjs; do
            echo "==> $test"
            node "$test"
          done

      - name: Validate release and repository
        shell: bash
        run: |
          node scripts/validate-userscript.mjs --base-ref origin/main
          python3 scripts/check_repository.py
          git diff --check

      - name: Commit production change
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Release v1.1.3 mission generation and journey capture"
          git push origin HEAD:fix/mission-generation-and-journey-capture
