name: Diagnose v1.0.30 release failure

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/release-v1030-status.yml'

permissions:
  actions: read
  contents: read
  pull-requests: write

jobs:
  diagnose:
    name: Report failed release log
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Comment failed log
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: '131'
          RUN_ID: '30166689627'
        shell: bash
        run: |
          set -euo pipefail
          gh run view "${RUN_ID}" \
            --repo "${GITHUB_REPOSITORY}" \
            --log-failed > /tmp/v1030-failed.log 2>&1 || true

          {
            echo '## v1.0.30 failed release log'
            echo
            echo '```text'
            tail -n 120 /tmp/v1030-failed.log
            echo '```'
          } > /tmp/v1030-failed.md

          gh pr comment "${PR_NUMBER}" \
            --repo "${GITHUB_REPOSITORY}" \
            --body-file /tmp/v1030-failed.md
