# Automatic verified release trigger

This permanent marker documents that Command Nexus release publication is checked from every push to `main`.

The publisher remains fail-closed: GitHub assets, checksum and Greasy Fork source parity must verify before the canonical single Discord LIVE announcement is posted.
